import React from "react";
import { CartSection } from "../sections";
import { useSelector } from "react-redux";

const Cart = () => {
  return (
    <main>
      <CartSection />
    </main>
  );
};

export default Cart;
